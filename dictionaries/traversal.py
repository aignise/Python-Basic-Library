def get_keys(d):
    """Return all keys from the dictionary."""
    return list(d.keys())

def get_values(d):
    """Return all values from the dictionary."""
    return list(d.values())

def get_items(d):
    """Return all key-value pairs (items) from the dictionary."""
    return list(d.items())
